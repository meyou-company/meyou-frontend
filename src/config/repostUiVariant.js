/**
 * Repost card UI variant (originalPostId + originalPost only).
 * Switch active variant here: 'A' | 'B' | 'C'
 *
 * A — icon ↗︎ near author/time (default)
 * B — subtle «↗︎ Поділилася/Поділився» above embed
 * C — subtle «✨ У стрічку» above embed
 */
export const REPOST_UI_VARIANT = "A";
